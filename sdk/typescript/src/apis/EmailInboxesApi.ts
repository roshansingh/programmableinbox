/* tslint:disable */
/* eslint-disable */
/**
 * ProgrammableInbox Email Inboxes API
 * Email inbox management endpoints with API key support
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type CreateEmailInbox201Response,
    CreateEmailInbox201ResponseFromJSON,
    CreateEmailInbox201ResponseToJSON,
} from '../models/CreateEmailInbox201Response';
import {
    type CreateEmailInboxRequest,
    CreateEmailInboxRequestFromJSON,
    CreateEmailInboxRequestToJSON,
} from '../models/CreateEmailInboxRequest';
import {
    type ErrorResponse,
    ErrorResponseFromJSON,
    ErrorResponseToJSON,
} from '../models/ErrorResponse';
import {
    type GetEmailInbox200Response,
    GetEmailInbox200ResponseFromJSON,
    GetEmailInbox200ResponseToJSON,
} from '../models/GetEmailInbox200Response';
import {
    type GetEmailInboxMessages200Response,
    GetEmailInboxMessages200ResponseFromJSON,
    GetEmailInboxMessages200ResponseToJSON,
} from '../models/GetEmailInboxMessages200Response';
import {
    type GetEmailInboxOtp200Response,
    GetEmailInboxOtp200ResponseFromJSON,
    GetEmailInboxOtp200ResponseToJSON,
} from '../models/GetEmailInboxOtp200Response';
import {
    type GetEmailMessage200Response,
    GetEmailMessage200ResponseFromJSON,
    GetEmailMessage200ResponseToJSON,
} from '../models/GetEmailMessage200Response';
import {
    type ListEmailInboxes200Response,
    ListEmailInboxes200ResponseFromJSON,
    ListEmailInboxes200ResponseToJSON,
} from '../models/ListEmailInboxes200Response';
import {
    type UpdateEmailInboxRequest,
    UpdateEmailInboxRequestFromJSON,
    UpdateEmailInboxRequestToJSON,
} from '../models/UpdateEmailInboxRequest';

export interface CreateEmailInboxOperationRequest {
    createEmailInboxRequest: CreateEmailInboxRequest;
}

export interface DeleteEmailInboxRequest {
    id: string;
}

export interface GetEmailInboxRequest {
    id: string;
}

export interface GetEmailInboxMessagesRequest {
    id: string;
    cursor?: string;
    limit?: number;
    threadId?: string;
    grouped?: boolean;
    q?: string;
    from?: string;
    tags?: Array<string>;
    categories?: Array<string>;
}

export interface GetEmailInboxOtpRequest {
    id: string;
    from?: string;
    withinMinutes?: number;
}

export interface GetEmailMessageRequest {
    id: string;
    messageId: string;
}

export interface UpdateEmailInboxOperationRequest {
    id: string;
    updateEmailInboxRequest: UpdateEmailInboxRequest;
}

/**
 * 
 */
export class EmailInboxesApi extends runtime.BaseAPI {

    /**
     * Creates request options for createEmailInbox without sending the request
     */
    async createEmailInboxRequestOpts(requestParameters: CreateEmailInboxOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['createEmailInboxRequest'] == null) {
            throw new runtime.RequiredError(
                'createEmailInboxRequest',
                'Required parameter "createEmailInboxRequest" was null or undefined when calling createEmailInbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKeyAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/emailInbox`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: CreateEmailInboxRequestToJSON(requestParameters['createEmailInboxRequest']),
        };
    }

    /**
     * Claims a new email address and returns the inbox. The address must be on a domain this account can receive at, and is immutable once created. Requires API key with `email_inboxes:create` scope. The inbox is always created in the organization the key is bound to — there is no way to name a different one.
     * Create an email inbox
     */
    async createEmailInboxRaw(requestParameters: CreateEmailInboxOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateEmailInbox201Response>> {
        const requestOptions = await this.createEmailInboxRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateEmailInbox201ResponseFromJSON(jsonValue));
    }

    /**
     * Claims a new email address and returns the inbox. The address must be on a domain this account can receive at, and is immutable once created. Requires API key with `email_inboxes:create` scope. The inbox is always created in the organization the key is bound to — there is no way to name a different one.
     * Create an email inbox
     */
    async createEmailInbox(requestParameters: CreateEmailInboxOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateEmailInbox201Response> {
        const response = await this.createEmailInboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for deleteEmailInbox without sending the request
     */
    async deleteEmailInboxRequestOpts(requestParameters: DeleteEmailInboxRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling deleteEmailInbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKeyAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/emailInbox/{id}`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Soft-deletes the inbox and its messages in one transaction. The rows are retained and every read filters them out, so the data is recoverable — but the **address is retired permanently and can never be claimed again, by anyone including you**. Mail keeps being delivered to it, so releasing it would hand the next claimant your still-arriving messages. Requires API key with `email_inboxes:delete` scope, which is granted separately from create and update for exactly this reason.
     * Delete an email inbox
     */
    async deleteEmailInboxRaw(requestParameters: DeleteEmailInboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.deleteEmailInboxRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Soft-deletes the inbox and its messages in one transaction. The rows are retained and every read filters them out, so the data is recoverable — but the **address is retired permanently and can never be claimed again, by anyone including you**. Mail keeps being delivered to it, so releasing it would hand the next claimant your still-arriving messages. Requires API key with `email_inboxes:delete` scope, which is granted separately from create and update for exactly this reason.
     * Delete an email inbox
     */
    async deleteEmailInbox(requestParameters: DeleteEmailInboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.deleteEmailInboxRaw(requestParameters, initOverrides);
    }

    /**
     * Creates request options for getEmailInbox without sending the request
     */
    async getEmailInboxRequestOpts(requestParameters: GetEmailInboxRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getEmailInbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKeyAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/emailInbox/{id}`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns a single email inbox by id, scoped to the organization the API key is bound to. Requires API key with `email_inboxes:read` scope.
     * Get an email inbox
     */
    async getEmailInboxRaw(requestParameters: GetEmailInboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetEmailInbox200Response>> {
        const requestOptions = await this.getEmailInboxRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetEmailInbox200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns a single email inbox by id, scoped to the organization the API key is bound to. Requires API key with `email_inboxes:read` scope.
     * Get an email inbox
     */
    async getEmailInbox(requestParameters: GetEmailInboxRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetEmailInbox200Response> {
        const response = await this.getEmailInboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getEmailInboxMessages without sending the request
     */
    async getEmailInboxMessagesRequestOpts(requestParameters: GetEmailInboxMessagesRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getEmailInboxMessages().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['cursor'] != null) {
            queryParameters['cursor'] = requestParameters['cursor'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['threadId'] != null) {
            queryParameters['threadId'] = requestParameters['threadId'];
        }

        if (requestParameters['grouped'] != null) {
            queryParameters['grouped'] = requestParameters['grouped'];
        }

        if (requestParameters['q'] != null) {
            queryParameters['q'] = requestParameters['q'];
        }

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['tags'] != null) {
            queryParameters['tags'] = requestParameters['tags'];
        }

        if (requestParameters['categories'] != null) {
            queryParameters['categories'] = requestParameters['categories'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKeyAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/emailInbox/{id}/messages`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns messages for a specific email inbox with optional pagination and filtering. Requires API key with `email_messages:read` scope.
     * Get messages from an email inbox
     */
    async getEmailInboxMessagesRaw(requestParameters: GetEmailInboxMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetEmailInboxMessages200Response>> {
        const requestOptions = await this.getEmailInboxMessagesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetEmailInboxMessages200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns messages for a specific email inbox with optional pagination and filtering. Requires API key with `email_messages:read` scope.
     * Get messages from an email inbox
     */
    async getEmailInboxMessages(requestParameters: GetEmailInboxMessagesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetEmailInboxMessages200Response> {
        const response = await this.getEmailInboxMessagesRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getEmailInboxOtp without sending the request
     */
    async getEmailInboxOtpRequestOpts(requestParameters: GetEmailInboxOtpRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getEmailInboxOtp().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['withinMinutes'] != null) {
            queryParameters['withinMinutes'] = requestParameters['withinMinutes'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKeyAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/emailInbox/{id}/otp`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns the most recently extracted one-time passcode (OTP) for an email inbox, with the message it came from. Requires API key with `email_messages:read` scope — this is a read of extracted message content, not inbox metadata. Shares its lookup and arguments with the pibx_email_get_latest_otp MCP tool.
     * Get the latest one-time code for an inbox
     */
    async getEmailInboxOtpRaw(requestParameters: GetEmailInboxOtpRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetEmailInboxOtp200Response>> {
        const requestOptions = await this.getEmailInboxOtpRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetEmailInboxOtp200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns the most recently extracted one-time passcode (OTP) for an email inbox, with the message it came from. Requires API key with `email_messages:read` scope — this is a read of extracted message content, not inbox metadata. Shares its lookup and arguments with the pibx_email_get_latest_otp MCP tool.
     * Get the latest one-time code for an inbox
     */
    async getEmailInboxOtp(requestParameters: GetEmailInboxOtpRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetEmailInboxOtp200Response> {
        const response = await this.getEmailInboxOtpRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getEmailMessage without sending the request
     */
    async getEmailMessageRequestOpts(requestParameters: GetEmailMessageRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling getEmailMessage().'
            );
        }

        if (requestParameters['messageId'] == null) {
            throw new runtime.RequiredError(
                'messageId',
                'Required parameter "messageId" was null or undefined when calling getEmailMessage().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKeyAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/emailInbox/{id}/messages/{messageId}`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));
        urlPath = urlPath.replace('{messageId}', encodeURIComponent(String(requestParameters['messageId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns a specific message from an email inbox. Requires API key with `email_messages:read` scope.
     * Get a single message
     */
    async getEmailMessageRaw(requestParameters: GetEmailMessageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<GetEmailMessage200Response>> {
        const requestOptions = await this.getEmailMessageRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => GetEmailMessage200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns a specific message from an email inbox. Requires API key with `email_messages:read` scope.
     * Get a single message
     */
    async getEmailMessage(requestParameters: GetEmailMessageRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<GetEmailMessage200Response> {
        const response = await this.getEmailMessageRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listEmailInboxes without sending the request
     */
    async listEmailInboxesRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKeyAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/emailInbox`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Returns a list of email inboxes for the organization. Requires API key with `email_inboxes:read` scope.
     * List email inboxes
     */
    async listEmailInboxesRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ListEmailInboxes200Response>> {
        const requestOptions = await this.listEmailInboxesRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ListEmailInboxes200ResponseFromJSON(jsonValue));
    }

    /**
     * Returns a list of email inboxes for the organization. Requires API key with `email_inboxes:read` scope.
     * List email inboxes
     */
    async listEmailInboxes(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ListEmailInboxes200Response> {
        const response = await this.listEmailInboxesRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for updateEmailInbox without sending the request
     */
    async updateEmailInboxRequestOpts(requestParameters: UpdateEmailInboxOperationRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling updateEmailInbox().'
            );
        }

        if (requestParameters['updateEmailInboxRequest'] == null) {
            throw new runtime.RequiredError(
                'updateEmailInboxRequest',
                'Required parameter "updateEmailInboxRequest" was null or undefined when calling updateEmailInbox().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ApiKeyAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/emailInbox/{id}`;
        urlPath = urlPath.replace('{id}', encodeURIComponent(String(requestParameters['id'])));

        return {
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: UpdateEmailInboxRequestToJSON(requestParameters['updateEmailInboxRequest']),
        };
    }

    /**
     * Updates an inbox display name. The address is immutable — submitting a different one is a 409; submitting the current one (after normalization) is an allowed no-op, so a client can PATCH a whole record back. Requires API key with `email_inboxes:update` scope.
     * Rename an email inbox
     */
    async updateEmailInboxRaw(requestParameters: UpdateEmailInboxOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreateEmailInbox201Response>> {
        const requestOptions = await this.updateEmailInboxRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreateEmailInbox201ResponseFromJSON(jsonValue));
    }

    /**
     * Updates an inbox display name. The address is immutable — submitting a different one is a 409; submitting the current one (after normalization) is an allowed no-op, so a client can PATCH a whole record back. Requires API key with `email_inboxes:update` scope.
     * Rename an email inbox
     */
    async updateEmailInbox(requestParameters: UpdateEmailInboxOperationRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreateEmailInbox201Response> {
        const response = await this.updateEmailInboxRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
